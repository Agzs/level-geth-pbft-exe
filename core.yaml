###############################################################################
#
#    Peer section
#
# //=>TODO. add this file referencing fabric/peer/core.yaml. --Agzs
###############################################################################
peer:
    # Path on the file system where peer will store data
    fileSystemPath: /usr/local/geth-pbft/hyperledgerRocksDB/signer1 # //=> should be changed for different OS. --Agzs
    # rocksdb configurations
    db:
        maxLogFileSize: 10485760
        keepLogFileNum: 10
        logLevel: "warn"

    profile:
        enabled:     false
        listenAddress: 0.0.0.0:6060
